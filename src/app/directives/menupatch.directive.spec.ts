import { MenupatchDirective } from './menupatch.directive';

describe('MenupatchDirective', () => {
  it('should create an instance', () => {
    const directive = new MenupatchDirective();
    expect(directive).toBeTruthy();
  });
});
